{
  "name": "hydrawise-proxy",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Lynden Garrett",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "docker:build": "docker build --platform ${PLATFORM:-linux/amd64} -t ${IMAGE:-hydrawise-proxy}:$npm_package_version -t ${IMAGE:-hydrawise-proxy}:latest .",
    "docker:push": "docker push ${IMAGE:-hydrawise-proxy}:$npm_package_version && docker push ${IMAGE:-hydrawise-proxy}:latest",
    "deploy": "npm run docker:build && npm run docker:push"
  }
}
